.DS_Store
*.backup
*.bak-*

# Agent memory is personal/dynamic (background, hardware, preferences) -
# useful locally across sessions, not meant for a published repo.
.claude/agent-memory/

# Personal hardware-specific setup notes (this machine only) - the public
# curriculum is platform-agnostic; this is the owner's own cheat-sheet.
MY_SETUP_*.md
